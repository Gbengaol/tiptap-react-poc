This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

This Project integrates [`@tiptap/react`](https://tiptap.dev/docs/editor/getting-started/install/react) package with some of it's most populat extensions.

The UI is built with [`shadcn/ui`](https://ui.shadcn.com/) and [`tailwind`](https://tailwindcss.com/).

## Getting Started

First, install the dependencies:

```bash
npm run install
# or
yarn install
# or
pnpm install
# or
bun install
```

Then, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

Have questions? Send an email to [`gbengacodes@gmail.com`](mailto:gbengacodes@gmail.com)
